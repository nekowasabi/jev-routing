# ツール選定比較実験の実行計画

## 目的

ローカル規則、常時 Jev、現行の混成選定、および無介入を同じ要求群で比較し、計測値に基づいて候補絞り込みの改善要否と採用条件を決める。

## 対象外

- 実験前の規則重み・閾値・ダッシュボード表示の変更
- 全ホストへの一括展開。各ホストは書換え可能性の事前確認に合格した場合だけ採点する
- 実行結果だけで規則を自動学習・自動変更する仕組み

## 工程一覧

| # | 内容 | 依存 |
|---|---|---|
| 01 | 正解ツールを含む代表要求群と採点契約を固定する | なし |
| 02 | 選定元を `local` / `jev` / `hybrid` で切り替え可能にする | 01 |
| 03 | 四条件の反復実行と集計を `x-cell` に組み込む | 01, 02 |
| 10 | 境界・異常系を含む自動検証と採用判定を実施する | 01, 02, 03 |

## 比較条件と採点契約

| 条件 | 選定 | カタログ |
|---|---|---|
| `baseline` | プロキシを使わない | 全候補 |
| `local` | ローカル規則だけ | 規則の候補だけ |
| `jev` | 各選定を Jev に委譲 | Jev の候補だけ。不確実・失敗時は全候補 |
| `hybrid` | 高確信はローカル、その他は Jev | 現行方式 |

すべてのプロキシ条件で `JEV_COMPACTION=off` と `JEV_REASONING=preserve` を固定し、選定以外の書換えを比較要因から除く。各要求にはホスト固有の正解ツール名を一つ以上付与し、正解ツール保持率の分母は「有効なラベルを持つ全実行」と実行前に固定する。比較可能なのは、同一コミット・同一モデル設定・同一要求・外部品質検証合格・プロキシ到達確認済みの実行だけとする。

採用候補は、`baseline` と同等以上の外部品質検証を満たし、正解ツール保持率を下げず、Jev 選定トークンが報告される場合はその量、未報告なら欠測を含めて比較表へ残す。単発の請求トークンや待ち時間だけでは採用しない。

## 全体の受入コマンド

```sh
go test ./internal/proxy ./internal/host ./internal/plan ./internal/compact
python3 -m unittest discover -s scripts -p 'test_summarize_selection_comparison.py'
python3 -m unittest discover -s scripts -p 'test_summarize_x_cell.py'
make test-selection-benchmark claude
```
