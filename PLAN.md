# Jevツール選択効率とダッシュボードの実装計画

## 目的

[最終改善案](docs/requirements/jev-gateway-final-improvement-proposal.md)を、独立検証できる実装切片へ分解する。
履歴・選択の信頼性、実通信の観測、読み取り専用画面を整備し、その後に強制選択・モデル分離・限定直接生成を追加する。
全工程は番号順の直列実行。各工程で実装と回帰テストを完結させ、テスト・文書・レビューのみの独立工程は設けない。

契約:
- 不確実・不正・未対応の要求は元要求へ戻す。低確信度で候補限定しない。
- 実験方式は既定無効。具体的なモデルや実APIの品質・料金を推測しない。
- 強制選択は実Jev回答を検証した適合要求だけ。ローカル採点を強制の根拠にしない。
- 画面は同一オリジン・ループバック限定・メモリ履歴。秘密情報は保存しない。
- 実ツール実行と承認はホストに残す。外部サービスへの再送や承認回避を追加しない。
- 各verifyはモック/固定入力で再実行できる。将来追加するテスト名は工程内で指定する。
- 既存の /stats と JEV_RUN_STATS の互換キーを維持する。
- 直接生成の初期範囲は明示許可されたChatの無引数/定数引数だけ。その他のプロトコルは既存上流へ転送。
- 第三者コードの参照元は jev-gateway commit 5dde23482c85dbdf273ffa026693426b1516be00。MIT表示を保持する。

## 対象外

- 本計画作成時の製品実装、実Jev/LLMによる有料評価、既定方式の自動昇格。
- 独立サービス、DB、永続イベント保存、他プロセス自動探索、公開ダッシュボード、画面からの変更。
- 大量カタログの多段選択、部分引数委譲、全候補への引数先行質問、自動再送。
- Cursor Connect RPC等の未対応バイナリ形式への最適化。
- 実ホストの承認処理を模擬試験だけで保証すること、未取得の価格から費用改善率を作ること。

実運用の品質・総費用・総時間は別途実測が必要であり、本計画の完了条件は実験機能と計測基盤の機械検証まで。
未決定のモデルは実行時の必須選択にせず、無指定なら元モデル、実験用許可リストは空とする。

## 工程一覧

| 番号 | 題 | 依存 |
|---|---|---|
| 01 | [呼び出しIDとGrokツール名の対応を修正](plan/process-01.md) | なし |
| 02 | [Responsesの正規化と介入対象の判定](plan/process-02.md) | 01 |
| 03 | [Jev入力予算と圧縮キャッシュの分離](plan/process-03.md) | 01, 02 |
| 04 | [Jev回答の検証と不確実時の無変更復帰](plan/process-04.md) | 02, 03 |
| 05 | [要求単位の判定履歴とJev実通信を計測](plan/process-05.md) | 04 |
| 06 | [上流JSONとSSEの使用量を逐次収集](plan/process-06.md) | 05 |
| 07 | [読み取り専用ダッシュボードの移植](plan/process-07.md) | 06 |
| 08 | [比較条件を独立指定する設定と集計出力](plan/process-08.md) | 07 |
| 09 | [適合する要求への強制選択を追加](plan/process-09.md) | 08 |
| 10 | [許可対象だけ引数生成モデルを分離](plan/process-10.md) | 09 |
| 11 | [無引数・定数引数だけの直接生成を限定実装](plan/process-11.md) | 10 |
| 12 | [比較結果の集計と外部完了判定を実装](plan/process-12.md) | 11 |

工程01〜07で信頼性・計測・画面を完成させる。08以降も同じ計画内で直列実行し、実験機能は無効のまま納品する。
工程12の固定課題は評価機能の検証用であり、実サービスでの高速化の証拠ではない。

## 全体の受入コマンド

リポジトリ直下で実行する。Nodeは画面の標準テストにだけ使用し、本体の実行依存には追加しない。

```sh
env -u TYPESAFE_API_KEY -u JEV_API_KEY go test -race -count=1 ./...
go vet ./...
node --test internal/proxy/assets/dashboard.test.mjs
python3 -m unittest discover -s scripts -p 'test_summarize_x_cell.py'
bash -n scripts/test-x-cell.sh
bash scripts/test-x-cell.sh --summarize scripts/testdata/x-cell
```

計画書の形式検証（現在実行する検査）:

```sh
python3 /Users/takets/repos/private_dotfiles/agents/skills/make-plan/tests/check-process-fields.py .
```

製品の受入コマンドは実装後に成功させるもの。作成時点で通過済みとは扱わない。

