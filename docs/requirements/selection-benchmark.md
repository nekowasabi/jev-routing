# ツール選定比較の採点契約

`scripts/testdata/selection-benchmark/input.json` は、ホスト別の要求、候補カタログ、正解ツール、外部成功条件を実行前に固定する。

- 正解ツールは空でなく、同じ要求のカタログに含まれる名前だけを許可する。
- 保持率の分母は、到達済みかつ書換え可能な正解ラベル付き実行の全件である。再試行を除外しない。
- `selection_tokens` は未報告を 0 にせず欠測として残す。
- 到達不能または書換え不能なホストは採点せず、除外理由を残す。
