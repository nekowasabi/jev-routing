# ライブツール検証シート

## 目的

Claude Code、Codex、Grok Build、Cursor CLI、Devin CLI のライブツールカタログと
`jev-routing` の選定・フィルタ結果が一致し、必要なツールが実行時に失われないことを
確認する。判定器の選定品質より、実行可能性を優先する。

このシートでは、次の失敗を別々に記録する。

1. 正規名とライブツール名の不一致
2. 位相判定による候補削減で、次の実行に必要なツールが外れること
3. ホスト側の権限・プロトコル・ツール提供の不備

## 合格条件

- 実行を要求するターンで、対象ホストが実際にコマンド実行ツールを呼べる。
- ライブ名が正規名と異なっても、同じ能力だと確認済みならライブ名へ投影される。
- 能力対応が不明または曖昧なら、ツールを削らず `alias_unresolved` として透過する。
- 検索→読取→実行の連続ターンで、後段の実行ツールが失われない。
- 失敗時に、ツール名・ルーティング判断・実際の呼び出し結果を同じ記録に残す。

## 事前準備

1. 対象の実行ファイルを更新する。

   ```bash
   make install
   ```

2. 各ホストを新規セッションで起動する。既存セッションは以前のカタログを保持する場合がある。
3. `~/.cache/jev-routing/run.log` の開始時刻を記録する。本文や秘密情報は共有しない。
4. 各ケースで、実行前のライブカタログ、`run.log` の `tools A→B chosen=...` 行、ホスト画面上の実ツール呼び出しを保存する。

## 正規名と想定ライブ名

| ホスト | 正規形からの選定名 | 実機で確認する候補名 |
| --- | --- | --- |
| Claude Code | `Bash` / `Read` / `Grep` / `Edit` / `Agent` | `Bash` / `Read` / `Grep` / `Edit` / `Agent` |
| Codex | `exec_command` / `read_file` / `grep_files` / `apply_patch` / `spawn_agent` | 実際に提供された名称 |
| Grok Build | `run_terminal_cmd` / `read_file` / `grep_search` / `task` | `run_terminal_command` / `grep` / `spawn_subagent` を含め実際に提供された名称 |
| Cursor CLI | `Shell` / `Read` / `Write` / `Task` | 実際に提供された名称と大文字小文字 |
| Devin CLI | `exec` / `read` / `edit` / `run_subagent` | `Shell` / `Read` / `Grep` / `Write` を含め実際に提供された名称 |

「想定ライブ名」は確定値ではない。実機で観測した名称を正本にする。

## 実機テストケース

| ID | 観点 | 操作 | 期待結果 | 記録欄 |
| --- | --- | --- | --- | --- |
| TC-01 | カタログ採取 | 新規セッションの開始直後に、提供ツール名を取得する | 正確な名称・大小文字・説明を保存できる | ホスト / セッションID / 名称 |
| TC-02 | 実行能力 | 「シェルで `printf tool-check` を実行し、出力だけ返す」と依頼する | 実行ツールが実呼び出しされ、`tool-check` を返す | ライブ名 / `chosen` / 実行結果 |
| TC-03 | 検索能力 | 既知の文字列を検索させる | 検索ツールが実呼び出しされる | ライブ名 / `chosen` |
| TC-04 | 読取能力 | TC-03 の結果で得たファイルを読ませる | 読取ツールが実呼び出しされる | ライブ名 / `chosen` |
| TC-05 | 位相遷移 | 「既知文字列を検索し、該当ファイルを読み、最後に `printf phase-check` を実行」と一つの依頼で実行する | 検索・読取後のターンで実行ツールが提供される | 各ターンの `tools A→B` / `chosen` |
| TC-06 | 明示実行 | コマンドを具体的に指定し、ほかの作業を含めない | 位相が locate でも実行能力を失わない | `tool_choice` の有無 / 実行結果 |
| TC-07 | 未解決名 | ライブ名と対応表が異なるホストで TC-02 を実施する | `alias_unresolved` 時はカタログが縮小されず、実行可能 | `tools A→A` / 理由 |
| TC-08 | ateam配送 | Codex と Grok Build を別 `run_id` で起動し、短い返信を依頼する | 別mailboxで受信し、通常TUIが表示される | `run_id` / mailbox / 返信 |
| TC-09 | 終了分離 | TC-08 の片方だけ `close <run_id>` を実行する | 他方のTUI・mailbox・状態は残る | 終了前後の状態 |

TC-02〜TC-07 は各ホストで実施する。Cursor CLI または Devin CLI が利用不可なら、
「未実施（実機なし）」とし、推測で合格にしない。

## 判定表

| ライブカタログ | ルーティング結果 | 実行結果 | 判定 | 次の処置 |
| --- | --- | --- | --- |
| 正規名と一致 | 必要ツールを保持 | 成功 | 合格 | 回帰試験へ記録 |
| 別名だが同じ能力 | ライブ名へ投影 | 成功 | 合格 | 別名回帰試験を追加 |
| 別名で対応不明 | `alias_unresolved`、全カタログ透過 | 成功 | 暫定合格 | 能力対応を一次資料で確認 |
| 別名で対応不明 | カタログが縮小 | 失敗または未実行 | 不合格 | 直ちに失敗開放へ修正 |
| 名称一致 | 位相で実行ツールが除外 | 未実行 | 不合格 | 位相ガードを修正 |
| 名称一致 | 実行ツールを保持 | 権限拒否・ホストエラー | 環境不合格 | ルーティングは変更せず、ホスト設定を修正 |

## 検証後の修正方針

### A. 正規名とライブ名が異なるが、同じ能力だと確認できた場合

1. `internal/host/host.go` の対応表は正規形のまま維持する。
2. `internal/proxy/rewrite.go` の `catalogAliasIn` に、**正規名→実測済みライブ名**の対応を追加する。
3. `internal/proxy/gateway_history_test.go` に、正規名入力でライブ名だけが残る回帰試験を追加する。
4. 実カタログを模した試験データに、名称・大小文字・プロトコル形式を固定する。

### B. 対応する能力が確認できない、または複数候補で曖昧な場合

対応表を推測で追加しない。`alias_unresolved` で全カタログを透過する。
これは Jev の取りこぼしを許容して、実行不能を避ける失敗開放である。

### C. 名称は一致するが、位相判定が実行ツールを外す場合

別名対応ではなく位相ポリシーの不具合として修正する。

1. 実行要求が明示されているターンは、実行能力を必須候補として保持する。
2. 検索→読取→実行の複合依頼では、Jev の選定結果を任意候補に限定し、必要能力の削除を許可しない。
3. 必要能力を確定できないときは `baseline` を選び、候補絞り込みをしない。
4. TC-05 と TC-06 を回帰試験に追加する。

### D. ateam の結果配送ターンの場合

配送には必ず実行能力が必要である。Grok Build の ateam 子ペインは `JEV_ROUTING_MODE=baseline`
で起動し、通常TUIを維持する。この例外を他ホストへ広げるのは、各ホストの TC-08 で
位相ガードの不足が実証された場合だけにする。

## 実装上の責務分担

| 層 | 責務 | 対象 |
| --- | --- | --- |
| 判断 | 正規形から必要能力を選ぶ | Jev / `internal/plan` |
| 投影 | 正規形をライブツール名へ解決する | `catalogAliasIn` |
| 安全策 | 解決不能時に候補を削らない | `alias_unresolved` |
| 実行 | ホストが提供する実ツールを呼ぶ | Claude Code / Codex / Grok / Cursor / Devin |
| 証拠 | カタログ・判断・実行結果を同時に残す | ホスト画面 / `run.log` / 回帰試験 |

Jev は候補選定だけを担い、ライブ名の照合・失敗開放・副作用の実行はコードが担う。
この分離により、判断品質の不確実さを実行不能へ伝播させない。

## 検証結果の記録テンプレート

```markdown
### <ホスト> / <TC-ID>

- 実施日時:
- バージョン:
- ライブツール名:
- `run.log`:
- 実ツール呼び出し:
- 結果: 合格 / 不合格 / 未実施
- 次の処置:
```

## 現在の自動検証

```bash
go test ./...
pytest -q /home/takets/repos/private_dotfiles/agents/skills/ateam/tests/test_ateam.py
```

これらは名称投影・未解決時の透過・ateamの実行分離を検証する。
各ホストの最新ライブカタログとの一致は、上記の実機テストで別途確認する。
